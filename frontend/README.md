
  # Define UI/UX Requirements

  This is a code bundle for Define UI/UX Requirements. The original project is available at https://www.figma.com/design/2HXxJoiQcNQumlzGbKXehz/Define-UI-UX-Requirements.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  